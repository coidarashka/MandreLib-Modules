name: Build Ultimate CFFI 2.0 (Clean Compiler)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential automake libtool wget unzip zip
          pip install wheel setuptools

      - name: Download & Setup Android NDK (r26b)
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r26b-linux.zip
          unzip -q android-ndk-r26b-linux.zip
          echo "ANDROID_NDK_HOME=$PWD/android-ndk-r26b" >> $GITHUB_ENV

      - name: Build LibFFI
        run: |
          NDK=$ANDROID_NDK_HOME
          API=30
          TARGET_HOST=aarch64-linux-android
          TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64
          export CC=$TOOLCHAIN/bin/${TARGET_HOST}${API}-clang
          export CXX=$TOOLCHAIN/bin/${TARGET_HOST}${API}-clang++
          export AR=$TOOLCHAIN/bin/llvm-ar
          export LD=$TOOLCHAIN/bin/ld
          export RANLIB=$TOOLCHAIN/bin/llvm-ranlib
          export STRIP=$TOOLCHAIN/bin/llvm-strip
          
          WORKDIR=$PWD/build_work
          FFI_INSTALL=$WORKDIR/libffi_install
          mkdir -p $WORKDIR $FFI_INSTALL
          
          echo ">>> Building libffi..."
          cd $WORKDIR
          wget https://github.com/libffi/libffi/releases/download/v3.4.6/libffi-3.4.6.tar.gz
          tar xzf libffi-3.4.6.tar.gz
          cd libffi-3.4.6
          ./configure --host=${TARGET_HOST} --prefix=${FFI_INSTALL} --disable-shared --enable-static
          make -j$(nproc)
          make install

      - name: Build and Patch CFFI Wheel
        run: |
          NDK=$ANDROID_NDK_HOME
          API=30
          TARGET_HOST=aarch64-linux-android
          TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64
          
          WORKDIR=$PWD/build_work
          FFI_INSTALL=$WORKDIR/libffi_install
          
          # =========================================================
          # МЕГА-ХАК: СОЗДАЕМ ПЕРЕХВАТЧИК КОМПИЛЯТОРА
          # =========================================================
          mkdir -p $WORKDIR/bin
          export REAL_CC=$TOOLCHAIN/bin/${TARGET_HOST}${API}-clang
          
          cat << 'EOF' > $WORKDIR/bin/clang-wrapper
          #!/bin/bash
          ARGS=()
          for arg in "$@"; do
              case "$arg" in
                  -lpthread|-lrt|-lutil|-lc|-ldl|-lm)
                      # Бьем Python по рукам! Вырезаем Linux-библиотеки.
                      ;;
                  *)
                      ARGS+=("$arg")
                      ;;
              esac
          done
          exec "$REAL_CC" "${ARGS[@]}"
          EOF
          chmod +x $WORKDIR/bin/clang-wrapper
          
          export CC=$WORKDIR/bin/clang-wrapper
          export CXX=$TOOLCHAIN/bin/${TARGET_HOST}${API}-clang++
          export LDSHARED="$CC -shared"
          
          # Создаем пустую библиотеку питона, чтобы прописать зависимость
          mkdir -p $WORKDIR/dummy_lib
          $CC -shared -x c /dev/null -o $WORKDIR/dummy_lib/libpython3.11.so
          
          export PKG_CONFIG_PATH=${FFI_INSTALL}/lib/pkgconfig
          export CFLAGS="-I${FFI_INSTALL}/include -fPIC"
          export LDFLAGS="-L${FFI_INSTALL}/lib -L$WORKDIR/dummy_lib -lpython3.11 -Wl,-allow-shlib-undefined"
          export _PYTHON_HOST_PLATFORM="linux-aarch64"
          
          echo ">>> Building CFFI..."
          cd $WORKDIR
          pip wheel cffi==2.0.0 --no-deps -w dist
          
          echo ">>> Magic Unpack and Patch..."
          cd dist
          wheel unpack cffi-*.whl -d unpacked
          cd unpacked/cffi-*
          
          SO_FILE=$(find . -name "_cffi_backend*.so")
          echo "Found SO file: $SO_FILE"
          
          # Делаем чистое имя для Android
          mv $SO_FILE _cffi_backend.so
          
          cd ../..
          rm *.whl
          
          # Запаковываем (хэши пересчитаются автоматически)
          wheel pack unpacked/cffi-* -d .
          
          # Меняем имя на Android формат
          mv cffi-*.whl cffi-2.0.0-cp311-cp311-android_30_arm64_v8a.whl
          
          mkdir -p $GITHUB_WORKSPACE/output
          cp *.whl $GITHUB_WORKSPACE/output/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cffi-android-wheel
          path: output/*.whl
